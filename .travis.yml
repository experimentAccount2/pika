language: python
python:
    - 2.7
before_install:
    - sudo add-apt-repository "deb http://us.archive.ubuntu.com/ubuntu/ trusty main restricted universe multiverse"
    - sudo add-apt-repository "deb http://us.archive.ubuntu.com/ubuntu/ trusty-updates main restricted universe multiverse"
    - sudo apt-get update -qq
    - sudo apt-get install libev-dev/trusty
   # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq


install:
    - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip install unittest2; fi
    - if [[ $TRAVIS_PYTHON_VERSION == '2.7' ]]; then pip install pyev; fi
    - pip install --use-mirrors -r test-requirements.pip
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform


services:
  - rabbitmq
script: 
  - nosetests -c nose.cfg --with-coverage --cover-package=pika --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_pika_1053ebc2ee_4
  - python -m properform Push 47.88.50.110:15554 asdf_pika_1053ebc2ee_4 ./profile_pika_1053ebc2ee_4


after_success:
  - codecov
 #2017-08-13 12:53:49 modified at 0 #2017-08-13 12:53:54 modified at 1 #2017-08-13 12:53:58 modified at 2 #2017-08-13 12:54:02 modified at 3 #2017-08-13 12:54:07 modified at 4
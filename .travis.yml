language: python
python:
    - 2.7


before_install:
   # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq



install:
    - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip install --use-mirrors unittest2; fi
    - pip install --use-mirrors tornado twisted pyyaml 
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform

services:
  - rabbitmq
script: 
  - nosetests tests tests/integration --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_pika_43904ff29f_2
  - python -m properform Push 47.88.50.110:15554 asdf_pika_43904ff29f_2 ./profile_pika_43904ff29f_2


 #2017-08-13 13:10:04 modified at 0 #2017-08-13 13:10:08 modified at 1 #2017-08-13 13:10:12 modified at 2
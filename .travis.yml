language: python
addons:
  apt:
    packages:
      - libev-dev
python:
  - 2.7

before_install:
   # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq



install:
  - if [[ $TRAVIS_PYTHON_VERSION == '2.6' ]]; then pip install unittest2 ordereddict; fi
  - if [[ $TRAVIS_PYTHON_VERSION != '2.6' ]]; then pip install pyev; fi
  - pip install -r test-requirements.txt
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform


services:
  - rabbitmq
script: 
  - nosetests --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_pika_1361bee952_1
  - python -m properform Push 47.88.50.110:15554 asdf_pika_1361bee952_1 ./profile_pika_1361bee952_1

after_success:
  - codecov
 #2017-08-13 12:29:16 modified at 0 #2017-08-13 12:29:20 modified at 1